# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
org.gradle.parallel=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class to reduce app footprint size
android.nonTransitiveRClass=true

# Fixed: Disabled local caching variants to stop GitHub Actions daemon thread execution faults
org.gradle.caching=false
org.gradle.configuration-cache=false

# Fixed: Explicitly enabled AndroidX support mapping ecosystem required by modern Compose BOM structures
android.useAndroidX=true
android.enableJetifier=true

# Set the maximum number of workers to 4 to avoid overloading the machine.
org.gradle.workers.max=4
